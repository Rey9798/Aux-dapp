import React from "react";
import ReactDOM from "react-dom/client";
import AuxApp from "./App";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AuxApp />
  </React.StrictMode>
);